[project]
name = "codex-deck-backend"
version = "0.1.0"
description = "Codex Deck backend domain foundation"
requires-python = ">=3.12"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
